{
  "name": "vanilla-js-ssr",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build && vite build --ssr src/entry-server.js --outDir dist/server",
    "start": "cross-env NODE_ENV=production node server.js",
    "vercel-build": "vite build && vite build --ssr src/entry-server.js --outDir dist/server"
  },
  "dependencies": {
    "@devwareng/vanilla-js": "^1.8.2",
    "@tailwindcss/vite": "^4.1.14",
    "animate.css": "^4.1.1",
    "express": "^5.1.0",
    "tailwindcss": "^4.1.14"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "vite": "^7.1.7"
  }
}